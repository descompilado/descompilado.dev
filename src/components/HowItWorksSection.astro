---
import {
    Puzzle,
    Terminal,
    Brain,
    Server,
    GitMerge,
    Braces,
} from "lucide-react";
import GoToJoinWaitlist from "./GoToJoinWaitlist.astro";

const steps = [
    {
        icon: Puzzle,
        title: "Escolha um Desafio",
        description:
            "Selecione uma tecnologia fundamental que você usa mas não entende completamente.",
    },
    {
        icon: Terminal,
        title: "Construa do Zero",
        description:
            "Siga nosso passo a passo e implemente cada componente linha por linha.",
    },
    {
        icon: Brain,
        title: "Domine os Fundamentos",
        description:
            "Compreenda profundamente como sistemas complexos funcionam por dentro.",
    },
];

const challenges = [
    {
        icon: Server,
        title: "Dns",
        description:
            'Descubra como a "lista telefônica da internet" funciona nos bastidores para conectar você aos servidores certos.',
    },
    {
        icon: GitMerge,
        title: "Git",
        description:
            "Implemente um sistema de versionamento e descubra os fundamentos por trás da arquitetura do Git",
    },
    {
        icon: Braces,
        title: "Json",
        description:
            "JSON.parse()... mas e por baixo dos panos? Crie seu próprio parser e domine a estrutura de dados mais popular da web.",
    },
];
---

<section id="como-funciona" class="py-24 bg-card/30">
    <div class="w-full max-w-[1400px] mx-auto px-6">
        <div class="max-w-6xl mx-auto">
            <h2
                class="text-3xl md:text-5xl font-bold text-center mb-4 animate-fade-in"
            >
                Sua Jornada para a <span class="gradient-text"
                    >Proficiência</span
                >
            </h2>
            <p
                class="text-xl text-muted-foreground text-center mb-16 max-w-2xl mx-auto animate-slide-up"
            >
                Um processo simples e estruturado para transformar você em um
                engenheiro de software completo
            </p>

            <div class="grid md:grid-cols-3 gap-8">
                {
                    steps.map((step, index) => {
                        const Icon = step.icon;
                        return (
                            <div
                                class="relative group animate-scale-in"
                                style={{ animationDelay: `${index * 0.2}s` }}
                            >
                                {index < steps.length - 1 && (
                                    <div class="hidden md:block absolute top-16 left-full w-full h-0.5 bg-gradient-to-r from-primary to-transparent transform translate-x-8 z-0" />
                                )}

                                <div class="relative h-full bg-card border border-border rounded-2xl p-8 text-center hover:shadow-card transition-all duration-300 group-hover:border-primary/50 z-10">
                                    <div class="absolute -top-4 left-1/2 transform -translate-x-1/2 w-8 h-8 bg-primary text-primary-foreground rounded-full flex items-center justify-center text-sm font-bold">
                                        {index + 1}
                                    </div>

                                    <div class="w-16 h-16 bg-primary/10 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:bg-primary/20 transition-colors">
                                        <Icon className="w-8 h-8 text-primary" />
                                    </div>

                                    <h3 class="text-xl font-semibold mb-4">
                                        {step.title}
                                    </h3>
                                    <p class="text-muted-foreground leading-relaxed">
                                        {step.description}
                                    </p>
                                </div>
                            </div>
                        );
                    })
                }
            </div>

            <div class="text-center mt-32 mb-16 space-y-8">
                <h2
                    class="text-3xl md:text-5xl font-bold text-center mb-4 animate-fade-in"
                >
                    Primeiros desafios <span class="gradient-text"
                        >chegando em breve...</span
                    >
                </h2>
            </div>

            <div class="grid md:grid-cols-3 gap-8">
                {
                    challenges.map((challenge, index) => {
                        const Icon = challenge.icon;
                        return (
                            <div
                                class="relative group animate-scale-in"
                                style={{ animationDelay: `${index * 0.2}s` }}
                            >
                                {index < steps.length - 1 && (
                                    <div class="hidden md:block absolute top-16 left-full w-full h-0.5 bg-gradient-to-r from-primary to-transparent transform translate-x-8 z-0" />
                                )}

                                <div class="relative h-full bg-card border border-border rounded-2xl p-8 text-center hover:shadow-card transition-all duration-300 group-hover:border-primary/50 z-10">
                                    <div class="w-16 h-16 bg-primary/10 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:bg-primary/20 transition-colors">
                                        <Icon className="w-8 h-8 text-primary" />
                                    </div>

                                    <h3 class="text-xl font-semibold mb-4">
                                        {challenge.title}
                                    </h3>
                                    <p class="text-muted-foreground leading-relaxed">
                                        {challenge.description}
                                    </p>
                                </div>
                            </div>
                        );
                    })
                }
            </div>

            <div class="xm:hidden flex flex-col items-center mt-16">
                <GoToJoinWaitlist position="como-funciona"/>
            </div>

        </div>
    </div>
</section>
